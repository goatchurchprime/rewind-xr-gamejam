# godot-xr-base-networking

from clean clone run 
>  godot4 --headless --xr-mode off -s plug.gd update debug
To export to android you will also need to install openxrvendorsplugin (v 4.0.0) from the assetlib
